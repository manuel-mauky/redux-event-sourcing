{
  "name": "redux-event-sourcing",
  "version": "0.0.1",
  "description": "Util to add event-sourcing features to redux apps",
  "license": "MIT",
  "module": "./dist/esm/index.js",
  "main": "./dist/cjs/index.js",
  "typings": "./dist/types/index.d.ts",
  "scripts": {
    "build": "yarn run clean && yarn run build:esm && yarn run build:types",
    "build:types": "tsc -p tsconfig.types.json",
    "build:esm": "tsc -p tsconfig.esm.json",
    "clean": "rimraf dist"
  },
  "peerDependencies": {
    "redux": "^4.0.0"
  },
  "devDependencies": {
    "redux": "^4.0.0",
    "rimraf": "^3.0.2",
    "typescript": "~4.4.3"
  },
  "dependencies": {
    "nanoid": "^3.1.28"
  }
}
